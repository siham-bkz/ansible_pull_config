- name: install cronjob (ansible-pull)
  cron:
    user: ansible
    name: "asible provision"
    minute: "*/10"
    job: "/usr/bin/ansible-pull -o -U https://github.com/siham-bkz/ansible_pull_config.git > /dev/null"
